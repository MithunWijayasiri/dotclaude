# Rules

Placeholder. Scoped instruction files are planned for this folder — see the roadmap in the [root README](../README.md).

A rule is a markdown file in `.claude/rules/` that carries project conventions. Rules can load lazily or always, and can be scoped to a glob so they only apply to matching files. They suit conventions too long for `CLAUDE.md` but too specific for a skill.
